const express = require('express');
const router = express.Router();

/*
 POST /hook/users/
 Creazione nuovo utenza
 stato ok → 200
 stato ko → 403, 404
 */


/*PUT /api/chat/user/{user-id}
 aggiornamento dei dati utente
 stati
 ok → 200
 ko → 400, 404, 422
 */

module.exports = router;